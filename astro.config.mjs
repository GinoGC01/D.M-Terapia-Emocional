import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://ginogc01.github.io',
    base: 'D.M-Terapia-Emocional',
    build: {
        assets: 'assets'
      }
});
